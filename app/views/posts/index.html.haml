.questions-container.whitish
  .search-panel.search
    = text_field_tag 'search',nil, placeholder: 'search nearby', value: (params[:query].present? ? params[:query] : nil), data: {url: posts_path(page: 1)},class: 'search-field'
    .suggestions.black.d-none
      
  .text-center.new-bussiness-lik
    .group-action
      - if current_user
        = link_to 'Add service', new_post_path, remote: true, class: 'new-group-post'
      - else
        = link_to 'Add service', new_user_registration_path(start: true), class: 'new-group-post'
  .clearfix


  .posts-container.posts
    - if @posts.length == 0
      .welcome
        %h1.text-center
          Sorry we could not find that service!
        %p.text-left
          This is a local search engin with web pages locally configured. Search nearby services or provide service to nearby people. You can contact to the other party directly through our tools.
          .clearfix

    = render partial: 'posts', locals: {posts: @posts}    
  .new-post