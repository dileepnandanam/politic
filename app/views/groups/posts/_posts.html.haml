- posts.each do |p|
  = render partial: 'groups/posts/post', locals: {post: p}
.more-responses
  - if @posts.next_page
    = link_to 'View more', group_posts_path(@group, page: (params[:page].present? ? params[:page].to_i + 1 : 2)), remote: true, class: 'view-more'
