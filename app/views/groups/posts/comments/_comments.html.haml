.comments
  - comments.each do |c|
    = render partial: 'groups/posts/comments/comment', locals: {expand: false, comment: c, new: false}
  .more-responses
    - if comments.next_page
      = link_to 'View more', group_post_comments_path(post.group, post, page: (params[:page].present? ? params[:page].to_i + 1 : 2)), remote: true, class: 'view-more'
