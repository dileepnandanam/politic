= simple_form_for([@quick_poll, @quick_poll_response]) do |f|
  = f.input :question_id, as: :radio_buttons, collection: @quick_poll.questions.map{|q| [q.id, q.name]}, label_method: :second, value_method: :first
  = f.submit 'Submit', class: 'btn-primary'
  .clearfix