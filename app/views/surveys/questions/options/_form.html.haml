= simple_form_for([@survey, @question, @option], html:{class: 'option-form', remote: true}) do |f|
  = f.input :name
  = f.submit 'submit', class: 'btn-primary'
  = link_to 'cancel', '#', class: 'cancel-form btn-primary pull-left', remote: true
  .clearfix